<h4>注文情報確認</h4>
<table class="table table-hover table-inverse">
  <thead>
  <tr>
    <th>商品名</th>
    <th>単価</th>
    <th>数量</th>
    <th>小計</th>
  </tr>
  </thead>
<tbody>
  <% @cart_items.each do |cart_item| %>
  <tr>
    <td><%= image_tag cart_item.item.image, size: "30x30" %><%= cart_item.item.item_name %></td>
    <td><%= cart_item.item.with_tax_price %></td>
    <td><%= cart_item.amount %></td>
    <td><%= cart_item.subtotal%></td>
  </tr>
  <% end %>
</tbody>
</table>
<br>送料<%= @shipping_cost %>
<br>商品合計金額:<%= @total %>
<br>請求金額<%= @total + @shipping_cost %>

<br>支払方法<%= @order.payment_method_i18n %>


<br>お届け先<%= @order.postcode %><%= @order.address %>
<br><%= @order.name %>
<%= form_with model: @order, url: orders_path, method: :post, local:true do |f| %>
<%= f.hidden_field :payment_method, :value => @order.payment_method %>
<%= f.hidden_field :postcode, :value => @order.postcode %>
<%= f.hidden_field :address, :value => @order.address %>
<%= f.hidden_field :name, :value => @order.name %>
<%= f.hidden_field :shipping_cost, :value => @shipping_cost %>
<%= f.hidden_field :total_payment, :value => @total + @shipping_cost %>
<%= f.submit "注文を確定する" %>
<% end %>