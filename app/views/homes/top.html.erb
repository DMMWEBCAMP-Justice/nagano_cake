<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
    <br><%= render 'public/genres/genre',genres: @genres %>
    </div>
    <div class='col-md-8 offset-md-1'style="background: url(<%= asset_path "DSC_0796.jpeg" %>);">
      <br><p class = "text-white d-flex align-items-center justify-content-center">ようこそ、ながのCAKEへ！
      <br>このサイトは、ケーキ販売のECサイトになります。
      <br>会員でない方も商品の閲覧はできますが、
      <br>購入には会員登録が必要になります。</p>
    </div>
        <h3>新着商品</h3>
     <div class ="row m-5">
        <% @items.each do |item| %>
        <div class="col-md-3">
          <div class="card">
              <%= link_to item_path(item.id) do %>
              <%= image_tag item.image, size: "180x150" %>
              <% end %>
          <p class="text-center"><%= item.item_name %>
         <br>¥<%= item.price %></p>
          </div>
        </div>
        <% end %>
     </div>
         <div><%=link_to items_path do %>
           全ての商品を見る＞
         <% end %></div>
  </div>
</div>
